const DropDownIcon = () => {
  return (
    <span className="pointer-events-none absolute inset-y-0 right-3 flex items-center transition duration-1000 ease-in-out group-hover:text-yellow">
      ▼
    </span>
  );
};

export default DropDownIcon;
