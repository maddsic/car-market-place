# car-management-v2 (JCC) API — copy to .env and adjust if needed
VITE_API_BASE_URL=http://localhost:3000
VITE_API_VERSION=/api/v1

# Legacy key (unused by Vite; kept for reference)
# API_BASE_URL=localhost:8080
